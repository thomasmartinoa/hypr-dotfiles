# GENERATED by hypr-theme — theme: {{ name }} ({{ mode }}). Do not edit.
# The Aether / Omarchy btop mapping, rendered from this rice's palette, so the
# two ways of colouring btop can be compared side by side (btop: Esc → Options
# → Color theme). "hypr-theme" is the rice's own mapping (btop.theme.tpl).
theme[main_bg]="{{ bg2 }}"
theme[main_fg]="{{ fg }}"
theme[title]="{{ fg }}"
theme[hi_fg]="{{ active }}"
theme[selected_bg]="{{ bg3 }}"
theme[selected_fg]="{{ fg }}"
theme[inactive_fg]="{{ accent_dim }}"
theme[graph_text]="{{ fg }}"
theme[meter_bg]="{{ bg3 }}"
theme[proc_misc]="{{ aqua }}"
theme[cpu_box]="{{ purple }}"
theme[mem_box]="{{ green }}"
theme[net_box]="{{ red }}"
theme[proc_box]="{{ active }}"
theme[div_line]="{{ accent_dim }}"
theme[temp_start]="{{ green }}"
theme[temp_mid]="{{ yellow }}"
theme[temp_end]="{{ red }}"
theme[cpu_start]="{{ aqua }}"
theme[cpu_mid]="{{ active }}"
theme[cpu_end]="{{ purple }}"
theme[free_start]="{{ purple }}"
theme[free_mid]="{{ active }}"
theme[free_end]="{{ aqua }}"
theme[cached_start]="{{ active }}"
theme[cached_mid]="{{ aqua }}"
theme[cached_end]="{{ purple }}"
theme[available_start]="{{ yellow }}"
theme[available_mid]="{{ red }}"
theme[available_end]="{{ red }}"
theme[used_start]="{{ green }}"
theme[used_mid]="{{ aqua }}"
theme[used_end]="{{ active }}"
theme[download_start]="{{ yellow }}"
theme[download_mid]="{{ red }}"
theme[download_end]="{{ red }}"
theme[upload_start]="{{ green }}"
theme[upload_mid]="{{ aqua }}"
theme[upload_end]="{{ active }}"
theme[process_start]="{{ aqua }}"
theme[process_mid]="{{ active }}"
theme[process_end]="{{ purple }}"
