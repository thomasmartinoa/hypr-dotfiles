general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display.
    inhibit_sleep = 3                           # wait until hyprlock is up before suspending.
}

# To pause ALL of this (downloads, long builds, ...) use caffeine:
#   SUPER+CTRL+I, or click the coffee icon in waybar.
# It takes a logind idle+sleep inhibitor, which hypridle honours
# (ignore_systemd_inhibit defaults to false), so no need to edit timeouts.

listener {
    timeout = 300                                # 5min.
    on-timeout = brightnessctl -d intel_backlight -s set 10%         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -d intel_backlight -r                 # monitor backlight restore.
}

# turn off keyboard backlight, comment out this section if you dont have a keyboard backlight.
listener { 
    timeout = 250                                          # 4min.
    on-timeout = brightnessctl -sd rgb:kbd_backlight set 0 # turn off keyboard backlight.
    on-resume = brightnessctl -rd rgb:kbd_backlight        # turn on keyboard backlight.
}

listener {
    timeout = 1800                                # 30min
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 2500                                                    # 42min
    on-timeout = hyprctl dispatch dpms off                            # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 3600                                # 60min
    on-timeout = systemctl suspend                # suspend pc
}
